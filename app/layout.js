export const metadata = {
  title: 'AeroSuite - Luxury Perfume Collection',
  description: 'Discover the essence of elegance with AeroSuite premium fragrances',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
